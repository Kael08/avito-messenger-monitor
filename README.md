# Avito Messenger Monitor

Приложение на базе Nest.js для мониторинга сообщений из Личного кабинета Авито через Puppeteer с трансляцией на фронтенд в реальном времени через WebSocket.

## 🚀 Возможности

- Автоматический вход в Личный кабинет Авито через Puppeteer
- Мониторинг сообщений от конкретного пользователя (Рушан Натфуллин / Рушан)
- Трансляция новых сообщений на фронтенд в реальном времени через WebSocket
- Обработка ошибок и корректное завершение работы
- Простой и удобный веб-интерфейс
- Поддержка Docker для удобного развёртывания

## 📋 Требования

- Node.js 18+ 
- npm или yarn
- Google Chrome/Chromium (устанавливается автоматически с Puppeteer)
- Cloudflared (для настройки туннеля, опционально)
- **Учетные данные от аккаунта Авито** (телефон и пароль)

## 🛠️ Установка и запуск

### Локальная установка

1. **Клонируйте репозиторий и установите зависимости:**

```bash
npm install
```

2. **Соберите приложение:**

```bash
npm run build
```

3. **Запустите приложение:**

```bash
npm run start:prod
```

Для разработки с автоматической перезагрузкой:

```bash
npm run start:dev
```

4. **Откройте браузер и перейдите на:**

```
http://localhost:3000
```

5. **В веб-интерфейсе введите:**
   - **Телефон**: номер телефона, привязанный к вашему аккаунту Авито (например: `+79001234567` или `89001234567`)
   - **Пароль**: пароль от вашего аккаунта Авито
   - Нажмите "Запустить мониторинг"

   ⚠️ **Важно**: Это должны быть ваши реальные учетные данные от аккаунта на Avito.ru. Приложение использует их для автоматического входа в ваш личный кабинет через Puppeteer.

### Запуск через Docker

1. **Соберите образ:**

```bash
docker-compose build
```

2. **Запустите контейнер:**

```bash
docker-compose up -d
```

3. **Просмотрите логи:**

```bash
docker-compose logs -f
```

## 🌐 Настройка туннеля Cloudflared (CloudPub)

Для проброса локального сервера наружу используйте Cloudflared:

### Установка Cloudflared

**Windows:**
```powershell
# Через Chocolatey
choco install cloudflared

# Или скачайте с https://github.com/cloudflare/cloudflared/releases
```

**macOS:**
```bash
brew install cloudflared
```

**Linux:**
```bash
# Ubuntu/Debian
wget https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64.deb
sudo dpkg -i cloudflared-linux-amd64.deb
```

### Запуск туннеля

**Важно**: Сначала запустите приложение, затем создайте туннель.

1. **В первом терминале запустите приложение:**
```bash
npm run start:dev
# или
npm run start:prod
```
Дождитесь сообщения: `Application is running on: http://localhost:3000`

2. **Во втором терминале создайте туннель:**

```bash
cloudflared tunnel --url http://localhost:3000
```

3. **Cloudflared создаст публичный URL, например:**
```
+--------------------------------------------------------------------------------------------+
|  Your quick Tunnel has been created! Visit it at:                                         |
|  https://random-words-1234.trycloudflare.com                                               |
+--------------------------------------------------------------------------------------------+
```

4. **Используйте этот URL для доступа к приложению извне**

⚠️ **Обратите внимание**: 
- Оба процесса (приложение и туннель) должны работать одновременно
- Туннель работает пока запущена команда `cloudflared tunnel`
- При закрытии терминала с туннелем URL перестанет работать

### Альтернативный способ (через cloudpub.ru)

Если у вас есть аккаунт на cloudpub.ru:

1. Зарегистрируйтесь на https://cloudpub.ru
2. Создайте новый туннель, указав локальный порт 3000
3. Получите публичный URL
4. Используйте его для доступа к приложению

## 🏗️ Архитектура

Приложение построено на базе Nest.js и использует модульную архитектуру с разделением ответственности:

### Компоненты:

1. **PuppeteerService** (`src/puppeteer/puppeteer.service.ts`)
   - Управляет жизненным циклом браузера Puppeteer
   - Выполняет автоматическую авторизацию в Авито по телефону и паролю
   - Мониторит новые сообщения от целевого пользователя ("Рушан Натфуллин" / "Рушан")
   - Использует множественные стратегии поиска сообщений (DOM парсинг, селекторы, MutationObserver)
   - Обрабатывает ошибки авторизации и сетевые сбои
   - Реализует корректное завершение работы через `OnModuleDestroy` интерфейс
   - Предотвращает дублирование сообщений через `Set` для отслеживания обработанных сообщений

2. **MessagesService** (`src/messages/messages.service.ts`)
   - Хранит полученные сообщения в памяти
   - Реализует паттерн Observer для подписки на новые сообщения
   - Предоставляет API для получения всех сообщений и подписки на новые

3. **MessagesGateway** (`src/messages/messages.gateway.ts`)
   - WebSocket Gateway на базе Socket.io
   - Реализует `OnGatewayConnection` и `OnGatewayDisconnect` для управления клиентами
   - Транслирует новые сообщения всем подключенным клиентам через `server.emit()`
   - Отправляет историю сообщений новым подключениям
   - Подписывается на события из MessagesService при инициализации

4. **AppController** (`src/app.controller.ts`)
   - REST API endpoints для управления мониторингом
   - `/start` - запуск мониторинга с учетными данными
   - `/stop` - остановка мониторинга
   - `/status` - получение статуса мониторинга
   - `/health` - проверка здоровья приложения

5. **Frontend** (`public/index.html`)
   - Простой веб-интерфейс на чистом HTML/CSS/JavaScript (без фреймворков)
   - Использует Socket.io Client для подключения к WebSocket серверу
   - Отображает сообщения в реальном времени с анимациями
   - Позволяет запускать/останавливать мониторинг через REST API
   - Показывает статус подключения WebSocket

### Поток данных:

```
Авито Messenger 
    ↓
Puppeteer Browser (автоматизированный Chrome)
    ↓
PuppeteerService (парсинг DOM, извлечение сообщений)
    ↓
MessagesService (хранение, управление подписками)
    ↓
MessagesGateway (WebSocket/Socket.io)
    ↓
Frontend (веб-интерфейс)
```

### Выбранные подходы:

1. **Модульная архитектура Nest.js**: Использование декораторов, Dependency Injection, модулей для чистой организации кода
2. **Множественные стратегии поиска**: Комбинация селекторов, DOM парсинга и MutationObserver для надежного обнаружения сообщений
3. **Observer Pattern**: Для развязки между PuppeteerService и WebSocket Gateway
4. **Graceful Shutdown**: Обработка сигналов SIGINT/SIGTERM и unhandled exceptions для корректного завершения
5. **Обработка ошибок**: Try-catch блоки на всех критических этапах с логированием
6. **Deduplication**: Использование Set для предотвращения дублирования сообщений

## 🔧 Конфигурация

### Переменные окружения

Создайте файл `.env` в корне проекта:

```env
PORT=3000
NODE_ENV=production
```

### Настройка Puppeteer

Puppeteer настраивается в `src/puppeteer/puppeteer.service.ts`. Основные параметры:

- `headless: false` - браузер виден (для отладки). Для продакшена можно установить `true`
- Таймауты и интервалы проверки сообщений настраиваются в методах сервиса

## 🐛 Решение проблем

### Проблема: Браузер не запускается

**Решение:** Убедитесь, что установлены все зависимости для Chromium. В Docker это уже настроено. Для локальной установки может потребоваться установка дополнительных библиотек системы.

### Проблема: Не получается авторизоваться в Авито

**Решение:** 
- Проверьте правильность телефона и пароля
- Убедитесь, что аккаунт не заблокирован
- Возможно, потребуется двухфакторная аутентификация - в этом случае код нужно будет ввести вручную в открывшемся браузере

### Проблема: Сообщения не отображаются

**Решение:**
- Проверьте, что мониторинг запущен (кнопка "Запустить мониторинг")
- Убедитесь, что в браузере открыт чат с нужным пользователем
- Проверьте консоль браузера на наличие ошибок
- Убедитесь, что WebSocket подключение установлено (статус "Подключено" в интерфейсе)

### Проблема: Туннель не работает

**Решение:**
- Убедитесь, что приложение запущено на порту 3000
- Проверьте, что порт не заблокирован файрволом
- Попробуйте перезапустить туннель

## 📝 API Endpoints

- `GET /` - Главная страница (веб-интерфейс)
- `GET /health` - Проверка здоровья приложения
- `GET /status` - Статус мониторинга
- `POST /start` - Запустить мониторинг (тело: `{ "phone": "...", "password": "..." }`)
- `POST /stop` - Остановить мониторинг

## 🔐 Безопасность

⚠️ **Важно:** Это тестовое приложение. В продакшене следует:

- Использовать HTTPS
- Добавить аутентификацию
- Хранить учетные данные в защищённом хранилище (не передавать через фронтенд)
- Добавить rate limiting
- Использовать переменные окружения для конфигурации

## 📄 Лицензия

MIT

## 👤 Автор

Разработано для тестового задания Productlab.pro

## 🤝 Поддержка

При возникновении вопросов или проблем, создайте issue в репозитории или свяжитесь с разработчиком.
